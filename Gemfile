source "https://rubygems.org"

# GitHub Pages가 실제로 사용하는 것과 동일한 버전 조합의 Jekyll + 플러그인 모음입니다.
# 로컬에서 `bundle install` 후 `bundle exec jekyll serve` 로 미리보기 할 수 있습니다.
gem "github-pages", group: :jekyll_plugins

# Ruby 3.x 환경에서 webrick이 기본 포함되지 않는 경우가 있어 명시적으로 추가합니다.
gem "webrick", "~> 1.8"
