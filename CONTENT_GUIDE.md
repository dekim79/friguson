# 콘텐츠 업데이트 가이드

이 문서는 개발 지식이 없어도 GitHub 웹사이트에서 바로 홈페이지 내용을 수정하는 방법을 설명합니다. 컴퓨터에 아무것도 설치하지 않고, 브라우저에서 GitHub 저장소에 로그인한 상태로 따라 하시면 됩니다.

모든 페이지는 `.md`(마크다운) 파일입니다. 파일 위쪽 `---`로 둘러싸인 부분을 **front matter**라고 부르며, 제목·설명 등 정보가 들어있습니다. 이 부분의 형식(콜론 `:`, 들여쓰기 등)은 최대한 그대로 유지하면서, 오른쪽 값만 바꾸는 것이 안전합니다.

## 1. GitHub 웹에서 파일 수정하기 (가장 쉬운 방법)

1. 저장소에서 수정할 파일을 클릭해서 엽니다. (예: `about.md`)
2. 오른쪽 위 연필 모양 아이콘(**Edit this file**)을 클릭합니다.
3. 내용을 수정합니다.
4. 아래로 스크롤하여 **Commit changes** 버튼을 누릅니다. (커밋 메시지는 "회사소개 문구 수정" 처럼 간단히 적으면 됩니다)
5. 1~2분 내로 실제 사이트에 반영됩니다. (GitHub Pages가 자동으로 다시 빌드합니다)

## 2. Experimental World에 새 글 올리기

1. 한국어 글은 `_experiments_ko` 폴더, 영어 글은 `_experiments_en` 폴더에 새 파일을 추가합니다.
2. 파일 이름은 영문 소문자와 하이픈(-)만 사용하세요. 예: `pressure-sensor-calibration.md`
   - 한국어/영어 글을 짝지어 올릴 계획이라면 두 폴더에서 **같은 파일 이름**을 쓰는 것을 추천합니다. (언어 전환 버튼이 자동으로 연결됩니다)
3. 아래 템플릿을 그대로 복사해서 새 파일에 붙여넣고 내용을 채우세요.

```markdown
---
title: "글 제목을 입력하세요"
category: 실험기법
date: 2026-07-18
excerpt: "목록 페이지 카드에 보여줄 한두 문장 요약입니다."
image: /assets/images/experiments/파일명.jpg
alt_url: /en/experimental-world/같은-파일이름/
---

여기부터 본문을 작성합니다. 마크다운 문법을 사용할 수 있습니다.

## 소제목

- 목록 항목
- 목록 항목

**굵은 글씨**, *기울임*, [링크 텍스트](https://example.com) 등을 사용할 수 있습니다.
```

- `category`는 보통 `실험기법` 또는 `계측장비` 중 하나를 사용합니다. (영어 글은 `Techniques` / `Instrumentation`)
- `date`는 `YYYY-MM-DD` 형식입니다. 최신 글이 자동으로 목록 위쪽에 노출됩니다.
- `image`는 선택 사항입니다. 없으면 기본 아이콘이 표시됩니다. 사진을 추가하는 방법은 아래 3번 항목을 참고하세요.
- `alt_url`은 반대 언어로 작성한 같은 글의 주소입니다. 아직 번역이 없다면 이 줄은 지워도 됩니다.

## 3. 사진/이미지 추가하기

1. `assets/images/` 폴더로 이동합니다. (Experimental World용 사진은 `assets/images/experiments/` 폴더를 새로 만들어 정리하는 것을 추천합니다)
2. **Add file → Upload files** 버튼으로 사진을 업로드합니다.
3. 파일 이름은 영문/숫자/하이픈으로 정리하세요. (예: `cold-plate-test-01.jpg`) 한글 파일명, 띄어쓰기는 피하는 것이 안전합니다.
4. 업로드한 이미지는 글이나 페이지에서 아래처럼 불러옵니다.

```markdown
![사진 설명](/assets/images/experiments/cold-plate-test-01.jpg)
```

**용량 팁**: 사진은 가능하면 가로 1600px, 용량 1~2MB 이하로 줄여서 올리면 사이트 로딩이 빨라집니다. 스마트폰 사진을 그대로 올려도 동작은 하지만, 용량이 크면 페이지가 느려질 수 있습니다.

## 4. 기존 페이지(회사소개, 기술 등) 문구 수정하기

`about.md`, `workbench.md`, `toolshop.md`, `workshop.md`, `contact.md` 등을 열어 원하는 문장을 바로 수정하면 됩니다. `<div>`, `<section>` 같은 태그로 둘러싸인 부분은 구조이니 태그 자체는 남겨두고, 태그 사이의 한글 문장만 바꾸는 것이 안전합니다. 영어 버전은 `en/` 폴더 안의 같은 이름 파일을 함께 수정해야 두 언어가 일치합니다.

## 5. 연락처 정보(전화번호, 주소) 추가하기

`contact.md`와 `en/contact.md` 안에는 전화번호·주소 블록이 주석 처리(`<!-- -->`)되어 숨겨져 있습니다. 공개하고 싶으면 해당 블록을 감싸는 `<!--` 와 `-->` 를 지우고, 안의 내용을 실제 정보로 바꾸면 됩니다.

## 6. 상단 메뉴 수정하기

`_data/nav.yml` 파일 하나에서 한국어(`ko:`)와 영어(`en:`) 메뉴를 관리합니다. 새 메뉴를 추가하려면 같은 형식으로 `title`과 `url`을 한 줄씩 추가하면 됩니다.

## 7. 로고/디자인을 바꾸고 싶다면

- 로고 이미지: `assets/images/friguson-wordmark-sm.png` (헤더/푸터), `assets/images/friguson-mark-512.png` (회사소개 페이지)
- 전체 색상: `assets/css/style.css` 파일 맨 위 `:root { ... }` 안의 색상 코드(`--navy`, `--red`, `--blue`, `--amber`)를 바꾸면 사이트 전체 색상이 함께 바뀝니다.

## 막히는 부분이 있다면

수정한 내용이 사이트에 이상하게 보인다면, GitHub 저장소의 **Actions** 탭(또는 Pages 배포 로그)에서 오류 메시지를 확인할 수 있습니다. front matter의 `---` 구간 형식이 깨지는 경우가 가장 흔한 원인이니, 콜론(`:`)과 따옴표가 원래 파일과 같은 모양인지 먼저 확인해보세요.
